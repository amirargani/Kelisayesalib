@{
    var faSelectVideoAfter = APPClinet.Messages.faMessage.SelectVideoAfter.ToString();
}
@*codepen.io/tibomahe/pen/qZbGEw*@
<!-- Upload Javascripts -->
<script type="text/javascript">
    (function () {
        $(".form-control-file").each(function () {
            var $input = $(this),
                $label = $input.next(".js-labelFile"),
                labelVal = $label.html();

            $input.on("change", function (element) {
                var fileName = "";
                if (element.target.value)
                    fileName = element.target.value.split("\\").pop();
                fileName
                    ? $label.addClass("has-file").find(".js-fileName").html("@faSelectVideoAfter" + " " + fileName)
                    : $label.removeClass("has-file").html(labelVal);
            });
        });
    })();
</script>