<script src="~/lib/js/dist/datepicker/js/bootstrap-datepicker.js"></script>
<script src="~/lib/js/dist/datepicker/locales/bootstrap-datepicker.de.min.js"></script>
<script>
        var day = @DateTime.Now.Day;
        var month = @DateTime.Now.Month;
        var year = @DateTime.Now.Year;
        $('.date').datepicker({
            format: 'yyyy/mm/dd',
            language: "de",
            autoclose: true,
            todayHighlight: true,
            todayBtn: true,
            beforeShowDay: function (date) {
                if (date.getMonth() == month - 1) {
                    if (date.getDate() <= day) {
                        return false;
                    }
                }
                if (date.getFullYear() == year) {
                    if (date.getMonth() < month - 1) {
                        return false;
                    }
                }
                if (date.getFullYear() < year) {
                    if (date.getDate() < 32) {
                        return false;
                    }
                }
            },
            beforeShowMonth: function (date) {
                if (date.getFullYear() == year) {
                    if (date.getMonth() < month - 1) {
                        switch (date.getDate()) {
                            default:
                            return false;
                        }
                        return false;
                    }
                }
                if (date.getFullYear() < year) {
                    switch (date.getMonth()) {
                        default:
                            return false;
                        //case 0:
                        //    return false;
                        //case 1:
                        //    return false;
                        //case 2:
                        //    return false;
                        //case 3:
                        //    return false;
                        //case 4:
                        //    return false;
                        //case 5:
                        //    return false;
                        //case 6:
                        //    return false;
                        //case 7:
                        //    return false;
                        //case 8:
                        //    return false;
                        //case 9:
                        //    return false;
                        //case 10:
                        //    return false;
                        //case 11:
                        //    return false;
                    }
                }
            },
            beforeShowYear: function (date) {
                if (date.getFullYear() < year) {
                    return false;
                }
            }
        });
</script>